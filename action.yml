name: 'Automatic Version Bumper'
description: 'Automatically bumps version and creates releases based on PR title conventions and CHANGELOG.md'
inputs:
  token:
    description: 'GitHub token for creating releases'
    required: true
    default: ${{ github.token }}
  draft:
    description: 'Create release as draft'
    required: false
    default: 'false'
  prerelease:
    description: 'Mark as prerelease'
    required: false
    default: 'false'
 
runs:
  using: 'composite'
  steps:
    - name: Get version from package.json
      shell: bash
      id: package-version
      run: |
        if [ -f package.json ]; then
          VERSION=$(node -p "require('./package.json').version")
          echo "PACKAGE_VERSION=v${VERSION}" >> $GITHUB_ENV
        fi
      continue-on-error: true
 
    - name: Get version from .version file
      shell: bash
      id: version-file
      run: |
        if [ -f .version ]; then
          VERSION=$(cat .version)
          echo "FILE_VERSION=v${VERSION}" >> $GITHUB_ENV
        fi
      continue-on-error: true
 
    - name: Get latest tag
      shell: bash
      id: get-latest-tag
      run: |
        git fetch --tags
        LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
        if [ ! -z "$LATEST_TAG" ]; then
          echo "LATEST_TAG=$LATEST_TAG" >> $GITHUB_ENV
        elif [ ! -z "$PACKAGE_VERSION" ]; then
          echo "LATEST_TAG=$PACKAGE_VERSION" >> $GITHUB_ENV
        elif [ ! -z "$FILE_VERSION" ]; then
          echo "LATEST_TAG=$FILE_VERSION" >> $GITHUB_ENV
        else
          echo "LATEST_TAG=v0.0.0" >> $GITHUB_ENV
        fi
        echo "Current version: $LATEST_TAG"
 
    - name: Determine version bump
      shell: bash
      id: version-bump
      run: |
        PR_TITLE="${{ github.event.pull_request.title }}"
        if [[ $PR_TITLE =~ ^feat!: || $PR_TITLE =~ ^fix!: || $PR_TITLE =~ ^BREAKING[[:space:]]CHANGE ]]; then
          echo "BUMP=major" >> $GITHUB_ENV
        elif [[ $PR_TITLE =~ ^feat: ]]; then
          echo "BUMP=minor" >> $GITHUB_ENV
        elif [[ $PR_TITLE =~ ^fix: ]]; then
          echo "BUMP=patch" >> $GITHUB_ENV
        else
          echo "BUMP=none" >> $GITHUB_ENV
        fi
        echo "PR Title: $PR_TITLE"
        echo "Bump Type: $BUMP"
 
    - name: Bump version
      if: env.BUMP != 'none'
      shell: bash
      id: bump-version
      run: |
        CURRENT_VERSION=${LATEST_TAG#v}
        IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"
        case $BUMP in
          major)
            NEW_VERSION="$((MAJOR + 1)).0.0"
            ;;
          minor)
            NEW_VERSION="${MAJOR}.$((MINOR + 1)).0"
            ;;
          patch)
            NEW_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))"
            ;;
        esac
        echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_ENV
        echo "New version: v$NEW_VERSION"

    - name: Extract release notes
      if: env.BUMP != 'none' && env.NEW_VERSION != ''
      shell: bash
      id: extract-notes
      run: |
        if [ -f CHANGELOG.md ]; then
          TEMP_FILE="release_notes.txt"
          echo "Searching for version: v${NEW_VERSION}"
          START_LINE=$(grep -n "^## \[${NEW_VERSION}\]" CHANGELOG.md | cut -d: -f1 || true)
          if [ -z "$START_LINE" ]; then
            START_LINE=$(grep -n "^## \[v${NEW_VERSION}\]" CHANGELOG.md | cut -d: -f1 || true)
          fi
          if [ -z "$START_LINE" ]; then
            START_LINE=$(grep -n "^## ${NEW_VERSION}" CHANGELOG.md | cut -d: -f1 || true)
          fi
          if [ -z "$START_LINE" ]; then
            START_LINE=$(grep -n "^## v${NEW_VERSION}" CHANGELOG.md | cut -d: -f1 || true)
          fi

          if [ ! -z "$START_LINE" ]; then
            echo "Found version section at line $START_LINE"
            NEXT_LINE=$(tail -n +$((START_LINE + 1)) CHANGELOG.md | grep -n "^## " | head -n 1 | cut -d: -f1 || echo "")
            if [ ! -z "$NEXT_LINE" ]; then
              END_LINE=$((START_LINE + NEXT_LINE - 1))
              sed -n "${START_LINE},${END_LINE}p" CHANGELOG.md > "$TEMP_FILE"
            else
              tail -n +${START_LINE} CHANGELOG.md > "$TEMP_FILE"
            fi
            echo "Extracted release notes:"
            cat "$TEMP_FILE"
          else
            echo "No matching version found in CHANGELOG.md, using PR details"
            {
              echo "## Release v${NEW_VERSION}"
              echo ""
              echo "### Changes"
              echo "- PR #${{ github.event.pull_request.number }}: ${{ github.event.pull_request.title }}"
              if [ ! -z "${{ github.event.pull_request.body }}" ]; then
                echo ""
                echo "### Description"
                echo "${{ github.event.pull_request.body }}"
              fi
            } > "$TEMP_FILE"
          fi
          if [ ! -s "$TEMP_FILE" ]; then
            echo "## Release v${NEW_VERSION}" > "$TEMP_FILE"
            echo "" >> "$TEMP_FILE"
            echo "### Changes" >> "$TEMP_FILE"
            echo "- Automated version bump" >> "$TEMP_FILE"
          fi
          echo "RELEASE_NOTES_PATH=${TEMP_FILE}" >> $GITHUB_ENV
        else
          {
            echo "## Release v${NEW_VERSION}"
            echo ""
            echo "### Changes"
            echo "- PR #${{ github.event.pull_request.number }}: ${{ github.event.pull_request.title }}"
            if [ ! -z "${{ github.event.pull_request.body }}" ]; then
              echo ""
              echo "### Description"
              echo "${{ github.event.pull_request.body }}"
            fi
          } > "release_notes.txt"
          echo "RELEASE_NOTES_PATH=release_notes.txt" >> $GITHUB_ENV
        fi
 
    - name: Create and push tag
      if: env.BUMP != 'none' && env.NEW_VERSION != ''
      shell: bash
      run: |
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"
        git tag -a "v$NEW_VERSION" -m "Release v$NEW_VERSION"
        git push origin "v$NEW_VERSION"
 
    - name: Create Release
      if: env.BUMP != 'none' && env.NEW_VERSION != ''
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ inputs.token }}
      with:
        tag_name: v${{ env.NEW_VERSION }}
        release_name: Release v${{ env.NEW_VERSION }}
        body_path: ${{ env.RELEASE_NOTES_PATH }}
        draft: ${{ inputs.draft }}
        prerelease: ${{ inputs.prerelease }}
